var msg;
msg="<p><code>The actual script is in external script file called common.js</code></p>";
function addno(headvar,bodyvar)
{
	document.write(""+msg)
	var sum=headvar+bodyvar
	return sum
}